/// <reference types="react-scripts" />

 /*

   /* Debugando 

    background: ${(props) => {
        console.log(props.isActive, '-' , props.activeColor);
    
        
        
    return (
        props.isActive 
        ? transparentize(0.9, colors[props.activeColor])
        : 'transparent'
        )

        
    }};

*/
